# Vielinks

Vielinks is a social media workspace for planning, publishing, analytics, and AI-assisted content workflows across Instagram, LinkedIn, and Facebook.

## Stack

- React 19
- TypeScript
- Vite
- Tailwind CSS v4
- React Router
- GSAP and Framer Motion

## Local Development

Install dependencies:

```bash
npm install
```

Start the development server:

```bash
npm run dev
```

Run lint:

```bash
npm run lint
```

Build for production:

```bash
npm run build
```

## Product Surfaces

- Landing page: `/`
- Pricing: `/pricing`
- FAQ: `/faq`
- Auth: `/login`, `/register`, `/forgot-password`, `/reset-password`
- Product pages: `/product/dashboard`, `/product/analytics`, `/product/scheduler`, `/product/ai-insights`, `/product/integrations`
- App dashboard: `/dashboard`

## Notes

The marketing site should avoid unverifiable proof claims until real customer evidence, reviews, uptime metrics, or case studies are available.
